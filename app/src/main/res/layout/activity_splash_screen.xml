<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent" android:background="#43428A"
    tools:context="com.csc413.team5.fud5.SplashScreenActivity">

    <!-- The primary full-screen view. This can be replaced with whatever view
         is needed to present your content, e.g. VideoView, SurfaceView,
         TextureView, etc. -->

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/imgLogo"
        android:layout_gravity="center"
        android:src="@drawable/fud5logo"
        android:paddingLeft="50dp"
        android:paddingRight="50dp"/>

    <RelativeLayout
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="left|bottom"
        android:padding="5dp">

        <ImageView
            android:layout_width="200dp"
            android:layout_height="wrap_content"
            android:id="@+id/imgLocu"
            android:layout_gravity="left|bottom"
            android:src="@drawable/poweredbylocu" />

        <ImageView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:id="@+id/imgYelp"
            android:layout_gravity="right|bottom"
            android:src="@drawable/poweredbyyelp"
            android:layout_toRightOf="@+id/imgLocu"
            android:paddingLeft="20dp" />
    </RelativeLayout>

</FrameLayout>
